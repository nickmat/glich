// object023.glcs  Test creating and using objects.

object obj {
    values first second;
    function fun.qual( arg ) {
        result = qual + " " + arg;
    };
};

let x = {obj};

write x @fun.hello( "world" );

/*[OUTPUT]
hello world
[OUTPUT]*/
