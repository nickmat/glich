// do005.glcs Test file to test do statement keeps track of line number

write "Start ";

let count = 0;
do { while count < 2;
  /* Multiline
   * comments
   */
  if count = 1 {
    xxx
  };
  let count += 1;
  // Single line comment
  write count,;
};

/*[OUTPUT]
Start 1, Error file:do005 (11): Unrecognised statement.

[OUTPUT]*/
