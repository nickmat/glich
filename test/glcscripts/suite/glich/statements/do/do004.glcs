// do004.glcs Test file to test do statement keeps track of line number

write "Start ";

let count = 0;
do {
  while count < 2;
  /* Multiline
   * comments
   */
  xxx
  let count += 1;
  // Single line comment
  write count,;
};

/*[OUTPUT]
Start Error file:do004 (11): Unrecognised statement.

[OUTPUT]*/
