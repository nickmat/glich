// do013.glcs Test file to test do block statement with comments

write "Start ";

let count = 0;
do { while count < 2;
  /* Multiline
   * comments
   */
  let count = count + 1;
  // Single line comment
  write count + ", ";
};

write "end";

/*[OUTPUT]
Start 1, 2, end
[OUTPUT]*/
