// do003.glcs Test file to test do statement keeps track of line number

write "Start ";

let count = 0;
do {
  while count < 2;
  /* Multiline
   * comments
   */
  let count += 1;
  // Single line comment
  write count,;
};

xxx
write "end";

/*[OUTPUT]
Start 1, 2, Error file:do003 (16): Unrecognised statement.

[OUTPUT]*/
