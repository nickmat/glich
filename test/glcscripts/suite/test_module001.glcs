// test_module001 - Test module, do not run separately.

object test_sum {
    values one two three;
    function sum {
        result = one + two + three;
    };
    function err {
        result = @error( "Testing." );
    };
};
